# strategy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build strategy` to build the library.

## Running unit tests

Run `nx test strategy` to execute the unit tests via [Jest](https://jestjs.io).
