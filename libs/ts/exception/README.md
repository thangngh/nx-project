# exception

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build exception` to build the library.

## Running unit tests

Run `nx test exception` to execute the unit tests via [Jest](https://jestjs.io).
